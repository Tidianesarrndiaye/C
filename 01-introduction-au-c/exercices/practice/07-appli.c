#include <stdio.h>

int main() {
    float C = 23 ; // temperature en Celsius
    float F = (float)  9 / 5 * C + 32; //temperature en Fahrenheit
    printf("La temperature (F): %f",F);
    return 0;
}